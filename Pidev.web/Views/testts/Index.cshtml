@model Pidev.data.testt
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<select>
    @foreach (var i in Model.listA)
    {
        <option>@i.nom</option>
    }
</select>

